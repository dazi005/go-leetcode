package main

import "fmt"

func sleep() (zhuangtai, shouyi int){
	return
}

func doSth() (zhuangtai, shouyi int){
	return
}

func main() {
	var x, a, b, n, t int
	fmt.Scan(&t)
	for i := 0; i < t; i++ {
		fmt.Scan(&x, &a, &b, &n)
	}
}
